import 'package:flutter/material.dart';

class RandomUser extends StatelessWidget {
  const RandomUser({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Random user page!');
  }
}